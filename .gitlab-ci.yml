variables:
  LUA: "lua-5.3.3"
  SRC: "lua-5.3.3/src"

step 1:
  stage: build
  before_script:
  - curl -R -O http://www.lua.org/ftp/$LUA.tar.gz
  - tar zxf $LUA.tar.gz
  - cd $LUA
  - make linux test
  - cd ..
  script:
  - make all LC="$SRC/luac" CFLAGS="-I$SRC" LDFLAGS="-L$SRC"
  artifacts:
    paths:
    - palantir

step 2:
  stage: test
  script:
  - ./palantir -v

step 3:
  stage: deploy
  script:
  - cp palantir ~/bin/
