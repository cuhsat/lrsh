build:
  stage: build
  before_script: 
    - echo "Starting..."
    - apt update && apt -y install make
  script: 
    - make all
  artifacts:
    paths:
      - palantir
  cache:
    paths:
      - "src/*.o"

test:
  stage: test
  script:
    - make test
